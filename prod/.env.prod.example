# ==============================================
# 프로덕션 환경 변수 설정
# ==============================================
# 사용법: 이 파일을 .env.prod로 복사 후 값 수정
#   cp .env.prod.example .env.prod
#   chmod 600 .env.prod
#   vi .env.prod

# ==============================================
# Spring Profile
# ==============================================
SPRING_PROFILES_ACTIVE=prod

# ==============================================
# Database (MariaDB Master/Slave via MaxScale)
# ==============================================
# DB 루트 비밀번호 (복제, MaxScale 설정용)
DB_ROOT_PASSWORD=CHANGE_THIS_STRONG_ROOT_PASSWORD

# 애플리케이션 DB 설정
DATABASE_NAME=auth_db
DATABASE_USER_NAME=auth_user
DATABASE_PASSWORD=CHANGE_THIS_STRONG_APP_PASSWORD

# 복제 사용자 (Slave → Master 연결용)
REPL_USER=repl_user
REPL_PASSWORD=CHANGE_THIS_REPLICATION_PASSWORD

# MaxScale 모니터/라우팅 사용자
MAXSCALE_MONITOR_USER=maxscale_monitor
MAXSCALE_MONITOR_PASSWORD=CHANGE_THIS_MONITOR_PASSWORD
MAXSCALE_ROUTER_USER=maxscale_router
MAXSCALE_ROUTER_PASSWORD=CHANGE_THIS_ROUTER_PASSWORD

# ==============================================
# Kafka
# ==============================================
KAFKA_URL1=kafka1:9091
KAFKA_URL2=kafka2:9092
KAFKA_URL3=kafka3:9093

# ==============================================
# Redis
# ==============================================
REDIS_HOST=redis
REDIS_PORT=6379

# ==============================================
# JWT
# ==============================================
# 최소 256비트(32자) 이상의 랜덤 문자열 사용
# 생성 명령어: openssl rand -base64 32
JWT_SECRET=4FEpNTMbUAEGgiru3VrEbwwJ+dib3lpG80e2rxHJOLM=
JWT_ACCESS_TOKEN_EXPIRE_TIME=3600000
JWT_REFRESH_TOKEN_EXPIRE_TIME=86400000

# ==============================================
# AES 암호화 키 (개인정보 암호화용)
# ==============================================
# 생성 명령어: openssl rand -base64 24
AES_ENCRYPTION_KEY=3P0LA07i6nvwIbAKLDN4lT5dWuLo3g6j
